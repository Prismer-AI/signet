name: Publish OpenClaw plugin

on:
  pull_request:
    paths:
      - "packages/signet-openclaw-plugin/**"
      - ".github/workflows/openclaw-plugin-publish.yml"
  workflow_dispatch:
  push:
    tags:
      - "openclaw-plugin-v*"

permissions:
  contents: read

jobs:
  build:
    name: Build plugin
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
      - name: Install workspace deps
        run: npm install --no-audit --no-fund
      - name: Build signet-node (transitive dep of openclaw plugin)
        working-directory: packages/signet-node
        run: npm run build
      - name: Build openclaw plugin
        working-directory: packages/signet-openclaw-plugin
        run: npm run build

  publish:
    name: ClawHub publish (tag or manual dispatch)
    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/openclaw-plugin-v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
      - name: Install workspace deps
        run: npm install --no-audit --no-fund
      - name: Build openclaw plugin
        working-directory: packages/signet-openclaw-plugin
        run: |
          (cd ../signet-node && npm run build)
          npm run build
      - name: ClawHub login
        env:
          CLAWHUB_TOKEN: ${{ secrets.CLAWHUB_TOKEN }}
        run: bunx clawhub login --token "$CLAWHUB_TOKEN"
      - name: ClawHub package publish
        run: |
          bunx clawhub package publish ./packages/signet-openclaw-plugin \
            --family code-plugin \
            --source-repo "${{ github.repository }}" \
            --source-commit "${{ github.sha }}" \
            --source-ref "${{ github.ref_name }}" \
            --source-path packages/signet-openclaw-plugin
