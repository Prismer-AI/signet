{
  "name": "@signet-auth/core",
  "version": "0.4.1",
  "description": "Cryptographic action receipts for AI agents",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/",
    "wasm/"
  ],
  "scripts": {
    "build": "npx tsc && rsync -a --exclude='.gitignore' wasm/ dist/wasm/",
    "test": "npx tsc && rsync -a --exclude='.gitignore' wasm/ dist/wasm/ && node --test dist/tests/core.test.js"
  },
  "license": "Apache-2.0 OR MIT",
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
